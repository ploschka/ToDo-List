{% extends 'base.html.twig' %}

{% block title %}ToDO List{% endblock %}

{% block body %}
<div class="content">
    <h1>ToDo List</h1>

    {{ form(todo_form) }}

    {% for todo in todos %}
        <p class="todo">{{ todo.text }}</p>
    {% endfor %}
</div>
{% endblock %}
